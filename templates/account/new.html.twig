{% extends 'base.html.twig' %}

{% block title %}inscription au site{% endblock %}

{% block body %}
    <div class="container_global_input wrapper">
        <h1 class="hight_title text_black">S'inscrire sur le site</h1>
        <div class="center_input">
        {{form_start(myForm)}}

            {{form_widget(myForm)}}
            <button type="submit" class="button_connexion">Confirmez l'Inscription</button>
        {{form_end(myForm)}}
        </div>
    </div>
{% endblock %}