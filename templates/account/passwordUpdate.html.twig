{% extends 'base.html.twig' %}

{% block title %}{% trans %}Change password{% endtrans %}{% endblock %}
{% block body %}
    <div class="container_global_input wrapper">
        <h1 class="hight_title text_black">{% trans %}Change password{% endtrans %}</h1>
        <div class="center_input">
        {{form_start(myForm)}}

        {{form_widget(myForm)}}
            <button type="submit" class="button_connexion">{% trans %}Confirm password change{% endtrans %}</button>
        {{form_end(myForm)}}
    
    </div>
    </div>
{% endblock %}