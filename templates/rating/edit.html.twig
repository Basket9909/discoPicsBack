{% extends 'base.html.twig' %}

{% block title %}Modifier la note {% endblock %}

{% block body %}
    <div class="container_global_input wrapper">
        <h1 class="hight_title text_black">Modifier la note</h1>
        <div class="center_input">
        {{ form_start(myForm) }}
            {{form_widget(myForm)}}
            <button type="submit" class="button_connexion">Modifier ma note</button>
        {{ form_end(myForm) }}
    </div>
    </div>
{% endblock %}
