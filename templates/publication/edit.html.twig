{% extends 'base.html.twig' %}

{% block title %}{% trans %}Edit spot{% endtrans %} {{publication.name}}{% endblock %}

{% block body %}
    <div class="container_global_input wrapper">
        <h1 class="hight_title text_black">{% trans %}Edit spot{% endtrans %} {{publication.name}}</h1>
        <div class="center_input">
        {{ form_start(myForm) }}
            {{form_widget(myForm)}}
            <button type="submit" class="button_connexion">{% trans %}Edit{% endtrans %}</button>
            
        {{ form_end(myForm) }}
    </div>
    </div>
{% endblock %}